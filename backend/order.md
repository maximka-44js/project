# 🚀 План поэтапной разработки для хакатона

## 📋 Приоритеты разработки (в порядке важности)

### **Этап 1: Базовая инфраструктура (30 минут)**
**Что делать:**
1. **Docker Compose** - поднять PostgreSQL и Redis
2. **API Gateway** - базовая структура с CORS
3. **Shared модули** - подключение к БД, базовые схемы
4. **Простая аутентификация** - генерация JWT токенов

**Почему первое:**
- Без этого ничего работать не будет
- Позволит сразу тестировать эндпоинты
- Команда может работать параллельно

---

### **Этап 2: User Service - Email подписка (45 минут)**
**Что реализовать:**
- `POST /api/v1/emails/subscribe`
- `GET /api/v1/emails/status/{email}`
- Модель EmailSubscription в PostgreSQL
- Валидация email и дедупликация

**Почему второе:**
- **Самый простой функционал** - только работа с БД
- **Первый пользовательский экшен** - можно сразу показать работающий продукт
- **Нет файлов, нет асинхронности** - меньше всего может сломаться
- Фронтенд может сразу интегрироваться

---

### **Этап 3: Resume Service - Загрузка файлов (60 минут)**
**Что реализовать:**
- `POST /api/v1/resumes/upload`
- `GET /api/v1/resumes/supported-formats`
- Валидация файлов (тип, размер)
- Сохранение в файловую систему
- Модель ResumeUpload в PostgreSQL

**Почему третье:**
- **Видимый прогресс** - пользователи могут загружать файлы
- **Нет ML/AI пока** - просто работа с файлами
- Можно заглушить анализ и показать статические результаты

---

### **Этап 4: Analysis Service - Заглушка результатов (30 минут)**
**Что реализовать:**
- `GET /api/v1/analysis/{analysis_id}`
- Модель Analysis в PostgreSQL
- **Статичные результаты** вместо реального анализа
- Имитация статусов: processing → completed

**Заглушка результатов:**
```json
{
  "position_levels": [
    {
      "level": "Middle Frontend Developer",
      "salary_min": 150000,
      "salary_max": 250000,
      "currency": "₽",
      "confidence": 0.87
    }
  ],
  "market_data": {
    "total_vacancies_analyzed": 1500,
    "data_freshness_days": 7,
    "location": "Москва"
  },
  "recommendations": [
    "Добавьте опыт работы с TypeScript",
    "Укажите знание фреймворков"
  ]
}
```

**Почему заглушка:**
- **MVP готов** - весь пользовательский флоу работает
- **Можно демонстрировать** на хакатоне
- **Фронтенд может полностью интегрироваться**

---

### **Этап 5: Настоящий анализ резюме (если останется время)**
**Что добавить:**
- Celery Worker для фоновой обработки
- Парсинг PDF/DOC файлов (PyPDF2, python-docx)
- Простой анализ текста (регулярки для навыков)
- Интеграция с API вакансий (HeadHunter API)

---

## 🛠️ Конкретный план действий

### **Час 1: Инфраструктура**
```bash
# 1. Создать docker-compose.yml
# 2. Поднять базы данных
docker-compose up -d postgres redis

# 3. Создать API Gateway с базовой аутентификацией
# 4. Создать shared/database.py и shared/schemas.py
```

### **Час 2: Email сервис**
```bash
# 1. User Service с FastAPI
# 2. Модель EmailSubscription 
# 3. Эндпоинты subscribe/status
# 4. Интеграция с API Gateway
# 5. Тестирование через Postman/curl
```

### **Час 3: Загрузка файлов**
```bash
# 1. Resume Service
# 2. Валидация и сохранение файлов
# 3. Модель ResumeUpload
# 4. Интеграция с API Gateway
# 5. Тестирование загрузки
```

### **Час 4: Заглушка анализа**
```bash
# 1. Analysis Service
# 2. Модель Analysis
# 3. Статичные результаты
# 4. Имитация задержки (sleep 10-30 сек)
# 5. Полное тестирование флоу
```

## ⚡ Критические моменты

**Что может сломаться:**
1. **CORS проблемы** - настроить в API Gateway
2. **Загрузка файлов** - проверить лимиты размера
3. **JWT токены** - простая реализация без refresh
4. **PostgreSQL миграции** - использовать SQLAlchemy.create_all()

**Запасной план:**
- Если что-то не работает - убрать аутентификацию временно
- Если файлы не загружаются - принимать только текст
- Если БД глючит - использовать in-memory словари

**Результат через 4 часа:**
- ✅ Полностью рабочий MVP
- ✅ Все эндпоинты из api.md
- ✅ Интеграция с фронтендом
- ✅ Готовый продукт для демо

Начинать точно с **Docker Compose + API Gateway**! Это фундамент всего.