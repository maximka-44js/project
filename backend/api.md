Вот дополненное описание API эндпоинтов с указанием триггеров:

## 📡 API Эндпоинты для реализации

### 1. 📧 Email сервис

#### **POST** `/api/v1/emails/subscribe`
**🔥 Триггер:** Пользователь нажимает кнопку "Начать анализ" в форме подписки на email

**Что должен делать эндпоинт:**
- Валидировать email адрес (формат, длина ≤ 254 символа)
- Проверить, не подписан ли уже этот email (дедупликация)
- Сохранить email в БД с метаданными (source, timestamp, user_agent)
- Вернуть информацию о подписке
- Логировать действие для аналитики

```json
Request:
{
  "email": "user@example.com",
  "source": "hero_section",
  "metadata": {
    "timestamp": "2025-11-21T10:00:00Z",
    "user_agent": "Mozilla/5.0..."
  }
}

Response:
{
  "data": {
    "id": "uuid-here",
    "email": "user@example.com", 
    "subscribed": true,
    "created_at": "2025-11-21T10:00:00Z"
  },
  "success": true,
  "message": "Email успешно добавлен"
}
```

#### **GET** `/api/v1/emails/status/{email}`
**🔥 Триггер:** Программная проверка статуса подписки (в данный момент используется только в хуке, но может быть вызвана для проверки существующих подписок)

**Что должен делать эндпоинт:**
- URL-декодировать email из параметра пути
- Найти подписку в БД по email
- Вернуть статус подписки (активна/неактивна)
- Обработать случай, когда email не найден (404)

```json
Response:
{
  "data": {
    "id": "uuid-here",
    "email": "user@example.com",
    "subscribed": true,
    "created_at": "2025-11-21T10:00:00Z"
  },
  "success": true
}
```

### 2. 📄 Резюме сервис

#### **POST** `/api/v1/resumes/upload`
**🔥 Триггер:** Пользователь выбирает файл резюме через file input или drag & drop в правой карточке

**Что должен делать эндпоинт:**
- Валидировать загруженный файл:
  - Проверить MIME тип (PDF, DOC, DOCX, TXT)
  - Проверить размер файла (≤ 10MB)
  - Проверить, что файл не пустой
- Сохранить файл в файловое хранилище (локально или S3)
- Создать запись в БД с метаданными файла
- **Автоматически запустить анализ** (добавить задачу в очередь)
- Вернуть ID загрузки и ID анализа
- Опционально связать с email, если передан

```
Content-Type: multipart/form-data

FormData:
- resume: File (PDF, DOC, DOCX, TXT)
- email: string (optional)

Response:
{
  "data": {
    "upload_id": "uuid-here",
    "file_name": "resume.pdf",
    "file_size": 1048576,
    "status": "uploaded", // или "processing"
    "analysis_id": "analysis-uuid" // если анализ запускается сразу
  },
  "success": true
}
```

#### **GET** `/api/v1/resumes/supported-formats`
**🔥 Триггер:** Загрузка страницы или инициализация компонента (для программной проверки поддерживаемых форматов)

**Что должен делать эндпоинт:**
- Вернуть статический список поддерживаемых расширений
- Можно кэшировать ответ

```json
Response:
{
  "data": [".pdf", ".doc", ".docx", ".txt"],
  "success": true
}
```

### 3. 🔍 Анализ сервис

#### **GET** `/api/v1/analysis/{analysis_id}`
**🔥 Триггер:** Автоматический поллинг каждые 10 секунд после успешной загрузки файла (до получения статуса `completed` или `error`)

**Что должен делать эндпоинт:**
- Найти анализ в БД по ID
- Вернуть текущий статус анализа:
  - `processing` - анализ в процессе
  - `completed` - анализ завершен, есть результаты
  - `error` - произошла ошибка при анализе
- Если статус `completed`, вернуть полные результаты анализа
- Если статус `error`, вернуть описание ошибки
- Обработать случай, когда анализ не найден (404)

**⚠️ Важно:** Фронтенд будет вызывать этот эндпоинт каждые 10 секунд до получения финального статуса

```json
Response (processing):
{
  "data": {
    "analysis_id": "uuid-here",
    "status": "processing"
  },
  "success": true
}

Response (completed):
{
  "data": {
    "analysis_id": "uuid-here", 
    "status": "completed",
    "results": {
      "position_levels": [
        {
          "level": "Junior Developer",
          "salary_min": 80000,
          "salary_max": 120000,
          "currency": "₽",
          "confidence": 0.85
        },
        {
          "level": "Middle Developer", 
          "salary_min": 150000,
          "salary_max": 250000,
          "currency": "₽",
          "confidence": 0.92
        }
      ],
      "market_data": {
        "total_vacancies_analyzed": 15420,
        "data_freshness_days": 30,
        "location": "Москва"
      },
      "recommendations": [
        "Добавьте больше технических навыков",
        "Укажите опыт работы с React"
      ]
    }
  },
  "success": true
}

Response (error):
{
  "data": {
    "analysis_id": "uuid-here",
    "status": "error", 
    "error_message": "Не удалось распознать текст резюме"
  },
  "success": false
}
```

#### **POST** `/api/v1/analysis/start` (опционально)
**🔥 Триггер:** Ручной запуск анализа, если он не запускается автоматически при загрузке файла (в текущей реализации не используется, но может быть полезен для отладки)

**Что должен делать эндпоинт:**
- Найти загруженный файл по upload_id
- Создать новую запись анализа в БД
- Добавить задачу анализа в очередь (Celery, RQ, etc.)
- Опционально связать с email для отправки результатов
- Вернуть ID созданного анализа

```json
Request:
{
  "upload_id": "uuid-here",
  "email": "user@example.com"
}

Response:
{
  "data": {
    "analysis_id": "uuid-here",
    "status": "processing"
  },
  "success": true
}
```

## 🎯 Пользовательский сценарий (User Journey)

1. **Пользователь заходит на страницу** → автоматически загружается компонент
2. **Вводит email и жмет "Начать анализ"** → `POST /api/v1/emails/subscribe`
3. **Успешная подписка** → показывается зеленое сообщение
4. **Выбирает файл резюме** → `POST /api/v1/resumes/upload`
5. **Файл загружен, анализ запущен** → автоматически начинается поллинг
6. **Каждые 10 секунд** → `GET /api/v1/analysis/{analysis_id}` до получения результатов
7. **Анализ завершен** → отображаются реальные зарплатные данные вместо примеров

## 📊 Ожидаемая нагрузка

- **Email подписка**: ~1-5 запросов в минуту на пользователя
- **Загрузка файлов**: ~1 запрос на пользователя за сессию  
- **Поллинг анализа**: ~6 запросов в минуту на анализ (каждые 10 сек)
- **Время анализа**: ожидается 1-5 минут (30-180 поллинг запросов)

Учитывайте это при настройке rate limiting! 🚀